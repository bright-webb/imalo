import { ArticleCarouselDirective } from './article-carousel.directive';

describe('ArticleCarouselDirective', () => {
  it('should create an instance', () => {
    const directive = new ArticleCarouselDirective();
    expect(directive).toBeTruthy();
  });
});
